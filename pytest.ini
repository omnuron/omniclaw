[pytest]
# Pytest configuration for OmniClaw tests

minversion = 6.0
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Test output options
addopts =
    -v
    --strict-markers
    --tb=short
    --color=yes
    -p no:warnings
        -p no:pytest_ethereum

# Markers for test organization
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow-running tests
    requires_api: Tests that require Circle API access
    cctp: CCTP-related tests
    x402: x402 protocol tests
    utils: Utility function tests
    live_postgres: Tests that require OMNICLAW_TEST_POSTGRES_DSN and a real Postgres database

# Asyncio configuration
asyncio_mode = auto

# Coverage options (if pytest-cov is installed)
[coverage:run]
source = src/omniclaw
omit =
    */tests/*
    */examples/*
    */scripts/*

[coverage:report]
precision = 2
skip_empty = True
show_missing = True
